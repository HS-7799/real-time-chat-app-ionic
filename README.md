# real-time-chat-app-ionic

it's multi-platform (web + mobile) application for chat.

## Quick start
Install the Ionic CLI with npm : 
```
npm install -g @ionic/cli
```

Install npm packeges :
```
npm install
```
Run the app : 
```
ionic serve
```
